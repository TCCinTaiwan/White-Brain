language: python

python:
  - 2.7.8
  - 2.7
  - pypy
  - 3.3
  - 3.4
  - 3.5
  - nightly
  - pypy3

install:
  - pip install --upgrade pip #[他預先的 pip 版本不是最新的，所以先更新一下]
  - pip install Flask #[這幾行都是在裝你需要的環境＆工具]

sudo: true

matrix:
    fast_finish: true

script:
    - python ./main.py

notifications:
  email:
    recipients:
      - john987john987@gmail.com
    on_success: always
    on_failure: always